import Button from "../components/osds/button"

# Buttons

<div class="osds-button-group">
  <Button buttonText="Primary Button" />
  <Button buttonType="secondary" buttonText="Secondary Button" />
  <Button buttonType="outline" buttonText="Alt Primary Button" />
  <Button buttonType="outline-secondary" buttonText="Alt Secondary Button" />
</div>

<h2 class="section-header">Component Source Code</h2>

```js
import React from "react"

function Button(props) {
  var classes = (function (buttonType) {
    return buttonType ? `osds-button osds-button--${buttonType}` : "osds-button"
  })(props.buttonType)

  return <button className={classes}>{props.buttonText}</button>
}

export default Button
```

<h2 class="section-header">Implementation</h2>

```js
<Button buttonType="secondary" buttonText="Secondary Button" />
```
