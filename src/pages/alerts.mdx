import Alert from "../components/osds/alert"

# Alerts

<Alert alertType="info" alertMessage="This is an example of an info message." />

<Alert
  alertType="success"
  alertMessage="This is an example of a success message."
/>

<Alert
  alertType="warning"
  alertMessage="This is an example of a warning message."
/>

<Alert
  alertType="danger"
  alertMessage="This is an example of a danger message."
/>

## Component Source Code

```js
import React from "react"

function Alert(props) {
  var alertIcon = (function (alertType) {
    switch (alertType) {
      case "info":
        return "fas fa-info-circle"
      case "success":
        return "fas fa-check-circle"
      case "warning":
        return "fas fa-exclamation-triangle"
      case "danger":
        return "fas fa-times-circle"
      default:
        return "no-icon"
    }
  })(props.alertType)

  return (
    <div className={`osds-alert osds-alert--${props.alertType}`} role="alert">
      <div className="osds-alert__icon">
        <span className={alertIcon}></span>
      </div>
      <div className="osds-alert__message">{props.alertMessage}</div>
    </div>
  )
}

export default Alert
```

## Implementation

```js
<Alert
  alertType="success"
  alertMessage="This is an example of a success message."
/>
```
